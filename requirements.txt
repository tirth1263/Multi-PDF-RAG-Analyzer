# The chain is built from langchain_core primitives (prompt | model | parser),
# so the `langchain` umbrella package and its legacy chain helpers aren't needed.
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0,<0.4.0
langchain-google-genai>=2.0.0,<3.0.0

streamlit>=1.40.0
PyPDF2>=3.0.1
faiss-cpu>=1.8.0
pandas>=2.0.0
Markdown>=3.5
python-dotenv>=1.0.0
